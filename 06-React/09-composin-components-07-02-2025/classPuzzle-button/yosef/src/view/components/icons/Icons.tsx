import { KeyRound  } from 'lucide-react';
import styles from "./Icons.module.scss"

// Usage
const Icons = () => {
  return <KeyRound className={styles.icon}  color="red" size={48} />;
};

export default Icons;